#1.  Declare age as integer variable 
my_age=20 

#2. Declare height as a float  variable 
my_height = 53

# 3. Declare a variable that store a complex number 
my_complex_number= 5+15j 

#4. Write a script that prompts the user to enter base 
# and height of the triangle and calculate an area of this triangle (area = 0.5 x b x h).
# Sccript to calculate the area of a triangle 
print("---Triangle Area Calculator ---")

# Prompt the user to enter base and height 
base_str=input("Enter the base of the triangle: ")
height_str=input("Enter the height of the triangle: ")

#Convert input strings to float numbers 
base= float(base_str) 
height=float(height_str)

#Calculate the area of the triangle 
area= 0.5 * base * height 

# Print the calculated area 
print(f"The area of the triangle is: {area}")

#5.Write a script that prompts the user to enter side a, side b,
# and side c of the triangle. Calculate the perimeter of the triangle (perimeter = a + b + c).

# Prompt the user to enter the length of side a
side_a=float(input("Enter the lenght of side a: "))

# Prompt the user to enter the length of side b 
side_b= float(input("Enter the length of side b: "))

# Prompt the user to enter the length of side c 
side_c=float(input("Enter the length of side c: "))

# Calculate the perimeter of the triangle 
perimeter= side_a + side_b +side_c

# Print the calcualted perimeter 
print(f"The primeter of the triangle is: {perimeter}")

#6. Get length and width of a rectangle using prompt. Calculate its area (area = length x width)
# and perimeter (perimeter = 2 x (length + width))

#Get length from the user input 
length=float(input("Enter the length of the rectangle: "))

# Get width from user input 
width=float(input("Enter the width of the rectangle: "))

# calculate the area 
area=length* width

#Calculate the perimeter
perimeter=2* (length+ width)

#Print the results 
print(f"\nArea of the rectangle: {area:.2f}")
print(f"Perimeter of the rectangle: {perimeter:.2f}")

#7.Get radius of a circle using prompt. Calculate the area (area = pi x r x r) 
# and circumference (c = 2 x pi x r) where pi = 3.14.

# Define the value of pi 
PI=3.14

# Get the radius from the user 
try:
    radius=float(input("Enter the radius of the circle: "))
except ValueError:
    print("Invalid input. Please enter a valid number.")
    radius=0
    exit()
# Calculate the area 
area= PI * radius * radius 

# Calculate the circumference 
circumference = 2 * PI * radius 

# Print the results 
print(f" The area of the circle is: {area:.2f}")
print(f"The circumference of the cricle is: {circumference:.2f}")

#8. Calculate the slope, x-intercept and y-intercept of y = 2x -2
# Define the equation parameters 
slope_m=2
y_intercept_b= -2

# Calculate the x intercept 
# Set y =0 and slove for x: 0= slope_m * x+y_intercept_b
#x= -y_intercept_b/ slope_m 
x_intercept = -y_intercept_b/ slope_m 

print(f"Slope: {slope_m}")
print(f"Y-intercept: (0, {y_intercept_b})")
print(f"x-intercept: ({x_intercept}, 0)")

# 9. Slope is (m = y2-y1/x2-x1). Find the slope and Euclidean distance between point (2, 2) and point (6,10)
import math 
# Define the points 
point1=(2,2)
point2=(6,10)

# Extract coordinates 
x1, y1 =point1
x2,y2=point2

#Calculate the slope (m=y2 - y1 / x2-x1)
slope=(y2-y1)/ (x2-x1)

# Calculate thes Eucliden distance 
#Distance =sqrt((x2-x1)^2+ (y2-y1)^2)
euclidean_distance= math.sqrt((x2 -x1)**2+ (y2-y1)**2)

print(f"The slope (m) between the points is: {slope}")
print(f"The eucliden distance between the points is: {euclidean_distance}")

# Compare the slopes in tasks 8 and 9 
print("---Comparison Results---")
if slope_m==slope: 
    print(f"The slopes are equal. Both slopes have a value of {slope_m}.")
elif slope_m> slope: 
    print(f"The first slope ({slope_m}) is greate than the second slope ({slope}.)")
else: 
    print(f"The second slope({slope}) is greater than the first slope({slope_m}).")

# 11. Calculate the value of y (y = x^2 + 6x + 9). Try to use different x values and figure out at what x value y is going to be 0
def calculate_y(x):
    """Calculates y for the equation y= x^2 + 6x+9"""
    y=x**2+6*x+9 
    return y 
# Test with different x values 
test_values=[-5,-4,-3,-2,-1,0,1]

print("---Calculating y for different x values---")
for x_val in test_values: 
    y_val=calculate_y(x_val)
    print(f"When x= {x_val}, y={y_val}")
    
print("\n--- Figuring out at what x value y is 0---")
# The equation has a single root at x= -3
# We can find this programmatically by iterating or using the quadratic formula 
found_root = False
for x_va in range(-10,10): #Check a range of integer values 
    y_val+calculate_y(x_val)
    if y_val == 0:
        print(f"Found the value: y is 0 when x = {x_val}")
        found_root= True
        break 
    if not found_root:
        print("Could not find an integer x value when y is 0 within the tested range.")

# 12. Find the length of 'python' and 'dragon' and make a falsy comparison statement.
length_python = len('python')
length_dragon = len('dragon')

print(f"Length of 'python': {length_python}")
print(f"Length of 'dragon': {length_dragon}")

#Falsy comparison statements (evalulate to false)
print(f"Comparison 'python' length > 'dragon' length: {length_python > length_dragon}")
print(f"Comparison 'python' length != 'dragon' length: {length_python != length_dragon}")
print(f"Comparison 'python' length < 'dragon' length: {length_python < length_dragon}")
# 13.Use and operator to check if 'on' is found in both 'python' and 'dragon'
word1= 'python'
word2= 'dragon'
substring = 'on'

# Use the 'and' operator to check for the substring in both strings 
is_present_in_both=(substring in word1) and (substring in word2)

print(f"Is '{substring}' in '{word1}'? {substring in word1}")
print(f"Is '{substring}' in '{word2}'? {substring in word2}")
print(f"Is '{substring}' present in both words? {is_present_in_both}")

# 14. I hope this course is not full of jargon. Use in operator to check if jargon is in the sentence.
# The sentence we want to check 
sentence= "I hope this course is not full of jargon"

# The word we are looking for 
word_to_find ="jargon"

# Use the in operator to perform the check 
if word_to_find in sentence:
    print(f"Yes, the word' {word_to_find}")
else: 
    print(f"No,the word '{word_to_find}")

# 15. There is no 'on' in both dragon and python
word1= "dragon"
word2="python"
segment="on"

if segment not in word1 and segment not in word2:
    print(f"Verified: There is no '{segment}' in both '{word1}' and '{word2}'.")
else: 
    print("Check failed.")

#16. Find the length of the text python and convert the value to float and convert it to string
# Define the text 
text="python"

# find the length of the text result is an integer: 6
length_int=len(text)
print(f"Length as integer: {length_int}, Type: {type(length_int)}")

# Covert the integer value to a float 
length_float = float(length_int)
print(f"Length as float: {length_float}")

#Covert the float to a string 
length_str=str(length_float)
print(f"Length as string: '{length_str}', Type: {type(length_str)}")

#17. Even numbers are divisible by 2 and the remainder is zero. How do you check if a number is even or not using python?
my_number =14 
# Check if the remainder of number /2 is equal to 0
if my_number % 2 == 0:
    print(f"{my_number} is an even number.")
else: 
    print(f"{my_number} is an odd number")

#18. Check if the floor division of 7 by 3 is equal to the int converted value of 2.7.
floor_division_result= 7//3
# Convert to integer useing the int() function to convert floating point number to an integer
int_converted_value=int(2.7)
# Compare the results use the equality operator == to check if the two values are equal
are_equal=(floor_division_result == int_converted_value)

# 19. Check if type of '10' is equal to type of 10
# check if the type of string '10' 
#is equal to the type of the integer 10 
comparison_result=type('10')==type(10)

print(f"The type of '10' is: {type('10')}")
print(f"The type of 10 is: {type(10)}")
print(f"Are the type equal? {comparison_result}")

#Check if int('9.8') is equal to 10
print(int('9.8')== 10)

# 21. Writ a script that prompts the user to enter hours and rate per hour. Calculate pay of the person?
# Script to calculate pay based on hours worked and rate per hour 
#input() returns a string, so we need to convert it to a float later.
hours_input = input("Enter hours worked: ")
rate_input=input("Enter Rate per Hour: ")
# Convert input strings to float number
try: 
    hours=float(hours_input)
    rate=float(rate_input)
    #total pay
    pay=hours * rate
    #Display the result 
    # we format the output to two decimal 
    print(f"\nPay: ${pay:.2f}")
except ValueError:
    print("Invalid input. Please enter valid numbers for hours and rate.")
    
# 22. Write a script that prompts the user to enter number of years. Calculate the number of seconds a person can live. Assume a person can live hundred years
# Calculate total potential lifespan in seconds, assuming a maximum age of 100 years.
# Define the assumed maximum lifespan in years
MAX_LIFESPAN_YEARS= 100

# Constants for calculations 
DAYS_PER_YEAR= 365.25 #Accounts for leap year
HOURS_PER_DAY= 24
MINUTES_PER_DAY= 60
SECONDS_PER_MINUTE=60 

# Calculate total seconds in a century 
total_seconds_in_century=(
    MAX_LIFESPAN_YEARS*
    DAYS_PER_YEAR*
    HOURS_PER_DAY*
    MINUTES_PER_DAY*
    SECONDS_PER_MINUTE
)
try:
    years_lived_input = input("Enter the number of years you have lived: ")
    years_lived = float(years_lived_input)

    if years_lived < 0:
        print("Error: Years lived cannot be negative.")
    elif years_lived > MAX_LIFESPAN_YEARS:
        print(f"Assuming maximum lifespan is {MAX_LIFESPAN_YEARS} years. Calculating potential remaining time.")
        years_remaining = 0 # If they lived longer than assumed maximum
    else:
        # 2. Calculate remaining years
        years_remaining = MAX_LIFESPAN_YEARS - years_lived
        #  Calculate remaining lifespan 
        # We resure the total seconds calculation structure,but for the remaining years
        # Reuse the total seconds calculation structure, but for the remaining years
        seconds_remaining=(years_remaining * DAYS_PER_YEAR * HOURS_PER_DAY * MINUTES_PER_DAY* SECONDS_PER_MINUTE )

        # Display the result 
        #Using comma formatting for readability of large numbers
        print("-" * 40)
        print(f"Assuming a maximum lifespan of {MAX_LIFESPAN_YEARS} years.")
        print(f"You have approximatley{years_remaining:.1f} years remaining.")
        print(f"You have approximately {seconds_remaining:,.0f} potential seconds left to live.")
        print("-" * 40)

except ValueError:
    # Handle cases where the user enters non-numeric input 
    print("\nError Please enter a vaild number for the years lived.")
    
# 23. Write a Python script that displays the following table
#Write a Python script that displays the following table
#1 1 1 1 1
#3 1 3 9 27
#5 1 5 25 125
for n in range(1,6): #Iterate from 1 to 5 (exclusive of 6)
    #Calculate n,n squared, and n cubed 
    n_squared= n ** 2
    n_cube= n**3 
    
    # Print the values for the current row 
    # The '1' is aconstant in the second column 
    print(f"{n} {n_squared} {n_cube}")
